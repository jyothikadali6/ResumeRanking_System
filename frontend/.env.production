# Backend API URL used at build time by Vite for production builds (Vercel).
# This is baked into the frontend bundle so no dashboard env var is needed.
VITE_API_URL=https://resume-ranker-api.onrender.com
